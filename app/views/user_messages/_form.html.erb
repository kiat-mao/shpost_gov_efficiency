<%= form_with(model: user_message, local: true) do |form| %>
  <% if user_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_message.errors.count, "error") %> prohibited this user_message from being saved:</h2>

      <ul>
        <% user_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
